# Отладка sdk-core

1. Запустить `pytest components/sdk-core/tests -q`.
2. Проверить регистрацию обработчиков в `SystemBus`.
3. Для пошаговой отладки использовать `python -m pdb` на тестах и точках интеграции.

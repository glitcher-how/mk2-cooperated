# Разработка sdk-core в Docker

Компонент подключается в контейнеры сервисов через общий контекст сборки репозитория.
Изменения в `components/sdk-core/src` автоматически попадают в system-образ при пересборке.

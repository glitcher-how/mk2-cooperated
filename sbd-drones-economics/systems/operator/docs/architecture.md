# Система operator

Система `operator` содержит два сервиса:

- `gateway` — внешний вход в систему;
- `operator_component` — бизнес-логика оператора БАС.

Для локальной разработки используется синхронная шина сообщений из `broker/`.
